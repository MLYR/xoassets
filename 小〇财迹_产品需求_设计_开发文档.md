# XOAssets / 小〇财迹 项目文档

## 0. 项目概述

### 0.1 项目定位

XOAssets / 小〇财迹 是一个面向个人用户的资产管理与财务复盘工具。

它不只是一个普通记账系统，而是用于帮助用户统一管理：

- 日常收入与支出
- 银行卡 / 支付宝 / 微信 / 现金账户
- 基金、股票、虚拟货币等投资资产
- 每日净资产变化
- 消费趋势
- 投资盈亏
- AI 财务日报 / 周报 / 月报

核心目标：

> 帮用户每天知道：钱花哪了、资产涨跌多少、净资产为什么变化。

---

# 一、需求文档

## 1. 背景

个人财务管理通常分为两类工具：

1. 记账类工具：只记录收入支出。
2. 投资类工具：只关注股票、基金、虚拟货币等投资收益。

但普通用户真正关心的是整体财务状态：

- 今天到底是赚了还是亏了？
- 工资、消费、基金、股票、虚拟货币加起来，总资产有没有增长？
- 哪些消费是不必要的？
- 哪些投资拖累了收益？
- 本月是否会超预算？

因此，本项目定位为：

> 个人资产全景看板 + 自动财务日报 + AI 财务分析助手。

---

## 2. 项目目标

### 2.1 产品目标

- 提供一个统一的个人资产管理入口。
- 支持用户记录日常收入、支出、转账、投资买入卖出。
- 支持查看基金、股票、虚拟货币等资产持仓。
- 每天生成资产变化统计。
- 通过图表展示支出、收入、投资收益、净资产变化。
- 通过 AI 总结用户每日 / 每周 / 每月财务情况。

### 2.2 技术目标

- 第一版以 Web / H5 响应式页面为主。
- 后端使用 Java Spring Boot。
- 数据库存储使用 MySQL。
- 行情数据支持定时同步。
- AI 报告支持异步生成。
- 保证数据结构后续可扩展到账户同步、小程序、App。

---

## 3. 用户角色

### 3.1 普通用户

主要使用功能：

- 记账
- 查看资产
- 查看图表
- 查看日报
- 设置预算
- 设置资产目标

### 3.2 管理员，后续可选

第一版可不做后台管理。

后续可支持：

- 用户管理
- 行情源管理
- 系统配置
- AI 模板配置
- 字典分类维护

---

## 4. 核心业务模块

| 模块 | 说明 | 优先级 |
|---|---|---|
| 用户模块 | 登录、注册、用户信息 | P0 |
| 账户模块 | 现金、银行卡、支付宝、微信、证券账户等 | P0 |
| 记账模块 | 收入、支出、转账、退款 | P0 |
| 分类模块 | 收支分类、自定义分类 | P0 |
| 投资持仓模块 | 基金、股票、虚拟货币持仓 | P0 |
| 行情价格模块 | 资产价格快照、涨跌幅 | P1 |
| 统计看板模块 | 支出图表、资产图表、收益图表 | P0 |
| AI 报告模块 | 日报、周报、月报 | P1 |
| 预算模块 | 月预算、分类预算、预算预警 | P1 |
| 资产目标模块 | 净资产目标、进度追踪 | P1 |
| 提醒模块 | 消费异常、预算超支、资产波动 | P2 |

---

## 5. 功能需求

## 5.1 登录注册

### 功能说明

用户可以通过账号密码登录系统。

### 功能点

- 用户注册
- 用户登录
- 用户退出
- 修改昵称
- 修改密码
- JWT 登录态认证

### 字段

| 字段 | 说明 |
|---|---|
| 用户名 | 登录账号 |
| 密码 | 加密存储 |
| 昵称 | 页面展示名称 |
| 手机号 | 后续可选 |
| 邮箱 | 后续可选 |

---

## 5.2 首页仪表盘

### 功能说明

首页展示用户当前整体财务状态。

### 核心指标

| 指标 | 说明 |
|---|---|
| 总资产 | 所有账户资产 + 投资资产 |
| 总负债 | 信用卡、贷款、借款等 |
| 净资产 | 总资产 - 总负债 |
| 今日支出 | 当日支出合计 |
| 今日收入 | 当日收入合计 |
| 今日投资盈亏 | 股票、基金、虚拟货币涨跌 |
| 今日净资产变化 | 今日净资产 - 昨日净资产 |

### 页面组件

- 顶部资产卡片
- 今日收支卡片
- 投资收益卡片
- 净资产趋势图
- 最近流水列表
- AI 今日财务总结

---

## 5.3 记账模块

### 功能说明

用户可以记录收入、支出、转账、退款等财务流水。

### 交易类型

| 类型 | 说明 |
|---|---|
| 支出 | 餐饮、购物、交通等 |
| 收入 | 工资、副业、奖金等 |
| 转账 | 账户之间资金流转 |
| 退款 | 支出退回 |
| 投资买入 | 买入基金、股票、币 |
| 投资卖出 | 卖出基金、股票、币 |

### 核心功能

- 新增流水
- 编辑流水
- 删除流水
- 查询流水
- 按时间筛选
- 按分类筛选
- 按账户筛选
- 按金额区间筛选
- 快速记账

### 快速记账示例

```text
中午吃饭 32
打车 26.5
工资 12000
买入 BTC 1000
卖出基金 3000
```

系统自动识别：

- 金额
- 交易类型
- 分类
- 账户
- 时间
- 备注

---

## 5.4 分类管理

### 功能说明

用户可以维护自己的收入支出分类。

### 默认支出分类

| 分类 | 示例 |
|---|---|
| 餐饮 | 吃饭、外卖、奶茶 |
| 交通 | 地铁、打车、公交 |
| 购物 | 网购、衣服、日用品 |
| 娱乐 | 游戏、电影、聚会 |
| 居住 | 房租、水电、物业 |
| 医疗 | 药品、看病 |
| 学习 | 课程、书籍 |
| 其他 | 无法归类 |

### 默认收入分类

| 分类 | 示例 |
|---|---|
| 工资 | 固定工资 |
| 奖金 | 年终奖、绩效 |
| 副业 | 外包、兼职 |
| 投资收益 | 分红、利息 |
| 红包 | 礼金、红包 |
| 退款 | 退款收入 |

---

## 5.5 账户管理

### 功能说明

用于记录用户的资金账户。

### 账户类型

| 类型 | 说明 |
|---|---|
| 现金 | 线下现金 |
| 银行卡 | 储蓄卡、工资卡 |
| 支付宝 | 支付账户 |
| 微信 | 微信零钱 |
| 信用卡 | 负债账户 |
| 证券账户 | 股票账户 |
| 基金账户 | 基金平台账户 |
| 虚拟货币钱包 | 钱包或交易所账户 |

### 功能点

- 新增账户
- 编辑账户
- 删除账户
- 设置账户余额
- 设置是否计入总资产
- 账户排序
- 查看账户详情和账户资金明细
- 查看账户余额曲线和支出分类统计
- 导出账户资金明细 CSV

---

## 5.6 投资持仓模块

### 功能说明

用户可以维护基金、股票、虚拟货币持仓。

### 支持资产类型

| 类型 | 示例 |
|---|---|
| 股票 | AAPL、TSLA、贵州茅台 |
| 基金 | 指数基金、主动基金 |
| 虚拟货币 | BTC、ETH、SOL |
| 其他 | 黄金、债券、REITs |

### 核心指标

| 指标 | 说明 |
|---|---|
| 持仓数量 | 股票股数、基金份额、币数量 |
| 成本价 | 平均买入成本 |
| 当前价 | 最新行情价 |
| 持仓市值 | 持仓数量 × 当前价 |
| 浮动盈亏 | 当前市值 - 持仓成本 |
| 盈亏比例 | 浮动盈亏 / 持仓成本 |
| 今日盈亏 | 今日价格变化带来的收益 |

### 功能点

- 新增持仓
- 编辑持仓
- 删除持仓
- 买入记录
- 卖出记录
- 成本价计算
- 当前市值计算
- 盈亏统计

---

## 5.7 统计图表模块

### 功能说明

通过图表展示用户财务变化。

### 图表类型

| 图表 | 说明 |
|---|---|
| 净资产趋势图 | 展示每日净资产变化 |
| 支出趋势图 | 展示每日 / 每月支出变化 |
| 收入趋势图 | 展示每日 / 每月收入变化 |
| 分类支出饼图 | 展示钱花在哪些分类 |
| 投资盈亏柱状图 | 展示不同资产盈亏 |
| 资产分布图 | 展示现金、基金、股票、币等占比 |
| 预算进度条 | 展示预算使用比例 |

---

## 5.8 AI 财务报告模块

### 功能说明

系统根据用户的收支、资产、投资数据，自动生成日报、周报、月报。

### 日报内容

- 今日支出合计
- 今日收入合计
- 今日投资盈亏
- 今日净资产变化
- 支出最高分类
- 投资贡献最大资产
- 异常消费提醒
- 简短建议

### 示例

```text
今日净资产增加 216.30 元。
其中投资收益贡献 342.80 元，生活支出减少 126.50 元。
今日主要支出为餐饮，共 58 元。
BTC 今日贡献收益 220 元，是今日收益主要来源。
本周外卖支出偏高，建议关注餐饮预算。
```

### 注意

AI 报告只做数据总结与风险提示，不提供明确投资买入 / 卖出建议。

---

## 5.9 预算模块

### 功能说明

用户可以设置月度预算和分类预算。

### 功能点

- 设置月总预算
- 设置分类预算
- 查看预算使用进度
- 预算超支提醒
- 预算即将超支提醒

### 预警规则

| 条件 | 提醒 |
|---|---|
| 使用超过 80% | 预算即将超支 |
| 使用超过 100% | 预算已超支 |
| 日均消费超过预算日均 | 消费速度偏快 |

---

## 5.10 资产目标模块

### 功能说明

用户可以设置净资产目标。

### 示例

```text
目标：年底净资产 200000 元
当前：152360 元
还差：47640 元
剩余时间：7 个月
平均每月需增加：6805.71 元
```

---

# 二、页面设计文档

## 1. 整体风格

### 1.1 设计关键词

- 简洁
- 专业
- 清晰
- 数据感
- 轻量
- 安全感

### 1.2 页面布局

第一版建议采用：

```text
左侧菜单 + 顶部用户信息 + 主内容区
```

适合 Web 和 H5 响应式。

移动端可切换为：

```text
底部 Tab + 卡片式内容
```

---

## 2. 页面清单

| 页面 | 路由 | 优先级 |
|---|---|---|
| 登录页 | /login | P0 |
| 注册页 | /register | P0 |
| 首页仪表盘 | /dashboard | P0 |
| 记账流水页 | /transactions | P0 |
| 新增记账页 | /transactions/create | P0 |
| 账户管理页 | /accounts | P0 |
| 投资持仓页 | /holdings | P0 |
| 统计分析页 | /analytics | P0 |
| AI 报告页 | /reports | P1 |
| 预算管理页 | /budgets | P1 |
| 资产目标页 | /goals | P1 |
| 分类管理页 | /categories | P1 |
| 系统设置页 | /settings | P2 |

---

## 3. 登录页

### 页面结构

```text
Logo / 产品名
账号输入框
密码输入框
登录按钮
注册链接
```

### 页面重点

- 简洁
- 不展示复杂信息
- 支持记住登录态

---

## 4. 首页仪表盘

### 页面目标

用户打开系统后，第一眼看到整体财务状态。

### 页面布局

```text
顶部：日期选择 / 用户信息

第一行：
- 总资产卡片
- 净资产卡片
- 今日支出卡片
- 今日投资盈亏卡片

第二行：
- 净资产趋势图
- 资产分布图

第三行：
- 今日财务日报
- 最近流水
```

### 卡片示例

```text
净资产
¥152,360.00
较昨日 +¥216.30 / +0.14%
```

### AI 总结卡片

```text
今日财务复盘
今日净资产增加 216.30 元。
投资收益表现较好，但餐饮消费略高于本月日均水平。
```

---

## 5. 记账流水页

### 页面目标

查看、筛选、管理所有收支流水。

### 页面布局

```text
顶部：新增流水按钮 / 快速记账输入框
筛选区：时间、类型、分类、账户、金额
列表区：流水列表
底部：分页
```

### 列表字段

| 字段 | 说明 |
|---|---|
| 时间 | 交易时间 |
| 类型 | 收入 / 支出 / 转账 / 投资 |
| 分类 | 餐饮、工资等 |
| 账户 | 支付账户 |
| 金额 | 正负金额 |
| 备注 | 用户输入备注 |
| 图片 | 用户上传的票据、截图等图片 |
| 操作 | 编辑 / 删除 |

---

## 6. 新增记账页 / 弹窗

### 推荐形式

第一版建议使用弹窗，不单独跳页。

### 表单字段

| 字段 | 类型 | 必填 |
|---|---|---|
| 交易类型 | 单选 | 是 |
| 金额 | 数字输入 | 是 |
| 分类 | 下拉选择 | 是 |
| 账户 | 下拉选择 | 是 |
| 交易时间 | 日期时间 | 是 |
| 备注 | 文本 | 否 |
| 图片 | 图片上传 | 否 |

### 快捷输入

```text
输入：中午吃饭 32
识别：支出 / 餐饮 / 32 元 / 今天
```

---

## 7. 账户管理页

### 页面目标

维护用户的所有资产账户。

### 页面布局

```text
顶部：新增账户按钮
主体：账户卡片列表
```

### 账户卡片字段

| 字段 | 说明 |
|---|---|
| 账户名称 | 工资卡、支付宝等 |
| 账户类型 | 银行卡、现金、信用卡等 |
| 当前余额 | 账户余额 |
| 是否计入总资产 | 是 / 否 |
| 更新时间 | 最近修改时间 |

账户编辑规则：

- 当前余额允许手动修改，用于利息、忘记记录流水等现实余额校准。
- 流水新增、修改、删除仍按事务联动账户余额。
- 点击账户卡片进入 `/accounts/:id` 账户详情页，展示普通收入、普通支出、转账转出、转账转入、退款、投资买入和投资卖出。
- 账户详情汇总按当前账户视角计算累计流入、累计流出和净流入；已撤销投资交易保留展示但不参与汇总。
- 账户详情统计区展示当前余额、累计流入、累计流出、余额修正、账户余额曲线和支出分类占比；不展示投资资金流向图。

---

## 8. 投资持仓页

### 页面目标

展示用户所有投资资产及盈亏。

### 页面布局

```text
顶部：币种展示下拉
第一行：投资总资产 / 持有收益 / 持仓成本 / 今日收益
第二行：基金 / 股票 / 虚拟货币模块入口
第三行：资产趋势（支持总览、基金、股票、虚拟货币切换）/ 投资分布
第四行：每日收益条形统计图（整行，基于所有持仓收益日历逐日汇总）
```

### 持仓列表字段

| 字段 | 说明 |
|---|---|
| 名称 | 股票、基金、币种名称 |
| 代码 | 资产代码 |
| 类型 | 股票 / 基金 / 币 |
| 币种 | CNY / USD |
| 持仓数量 | 数量 |
| 成本价 | 平均成本 |
| 当前价 | 最新价格 |
| 市值 | 当前市值 |
| 浮动盈亏 | 盈亏金额 |
| 盈亏率 | 盈亏比例 |
| 今日收益 / 收益率 | 今日有效价格 / 净值存在时展示；缺失时展示 `--` |
| 昨日收益 | 独立列展示昨日收益，不能塞进今日收益单元格 |
| 操作 | 买入 / 卖出 / 编辑 / 删除 |

前端交互规则：

- 不再区分“资产”和“持仓”两个入口，用户只新增或编辑持仓。
- 持仓表单内维护资产名称、资产代码、资产类型、市场、币种、行情来源和行情键。
- 后端仍保留 `xo_asset` 作为行情刷新和价格快照的内部基础表。
- 第三方行情只允许后端 `QuoteProvider` 调用，前端只调用 XOAssets 后端接口。
- 新增持仓支持 `GET /api/assets/lookup` 自动识别资产信息；用户选择查询结果后自动填充资产名称、代码、市场、币种、行情来源、行情键、当前价格和行情时间。
- 基金新增持仓允许先保存 0 份额，用于后续通过金额买入和确认净值自动生成真实份额；股票、虚拟货币仍要求用户录入有效数量。
- 投资主页拆成总览 / 基金 / 股票 / 虚拟货币四个页内模块；底层仍使用统一持仓、统一交易和统一价格模型。
- 投资主页总览不再展示单持仓收益贡献柱状图，改为每日收益条形统计图；每日收益与持仓详情收益日历保持同一口径，按日期把所有持仓的 `profitAmount` 汇总，不用趋势快照、累计收益差或其他字段估算；聚合接口失败时不展示旧口径或不完整汇总。
- 投资总览、模块卡片和首页的昨日投资盈亏也复用同一套收益日历同日汇总口径；展示今天之前最近一个有收益日，前端文案标注为“上一收益日”，确保周末 / 节假日后仍能展示最近可比收益，并和单持仓收益日历逐项相加一致。
- 持仓列表和持仓详情中的昨日收益同样取单持仓收益日历里今天之前最近一个有收益日，基金今日净值更新后不得把今日收益误显示到昨日收益列。
- 后端按资产类型和名称派生 `assetSubType`、`profitDisplayMode`、`valuationMode`、`tradeVenue`，前端今日收益必须严格使用今日有效价格 / 净值；今日价格未更新时展示 `--`，不得把昨日净值或最近价写入今日收益。
- 投资总览、模块卡片、模块趋势和全持仓每日收益等跨持仓聚合字段统一先折算成人民币，Web 再按顶部币种下拉切换展示；单持仓详情和单持仓收益日历仍保留资产原币种。
- 投资总览中今日收益按 `todayPriceAvailable=true` 动态汇总，基金晚间净值更新后自然进入今日收益；持有收益统计全部资产。
- Web 不再提供 `/investments/details` 混合明细页，基金、股票、虚拟货币分别在各自模块中展示和新增持仓；新增持仓弹框中资产类型由当前模块锁定。
- `/investments/holdings/:id` 为投资持仓详情页，顶部统计块一行三列展示当前市值、持仓数量、持仓成本、今日收益、总收益、回本涨幅；不单独展示“总收益率”和“已实现盈亏”统计块。持仓数量卡底部不展示无意义趋势，持仓成本卡底部展示成本价；交易记录前端默认 10 条分页，可切换 10 / 50 / 100 / 300；收益日历按日历格直接展示每天收益，净值型基金按净值日后的下一交易日展示，走势默认展示总市值，可在右上角切换总市值 / 价格，并复用买入、卖出、手动价格、刷新行情和撤销交易能力。
- 买入 / 卖出必须选择当前用户资金账户；买入扣款，卖出到账，不写入普通收支流水。
- 基金金额买入使用“实际买入时间”，前端调用确认日预估接口展示 15:00 后、非交易日顺延和 QDII T+2 提示；保存时仍只提交 `transactionTime`。
- 虚拟货币新增时默认使用 USD；页面默认以人民币展示，可通过下拉切换 USD，USD/CNY 汇率从后端日缓存读取，页面不再展示手工汇率输入。
- 投资数量最多保留 10 位小数；手续费、成本、市值、浮动盈亏和收益率统一按 4 位小数输入、计算和展示。
- 行情价格快照保留 8 位；CRYPTO 当前价至少展示 6 位，FUND / STOCK 当前价展示 4 位；基金优先用天天基金 F10 历史净值表获取最新单位净值和上一交易日单位净值。
- 手动更新基金净值时同时写入当前价和 `xo_asset_price_daily`，保证确认、收益日历和资产快照使用同一日级净值口径。
- 价格快照统一记录 `previous_close`、`change_amount`、`change_percent`、`market_status`，缺失部分行情字段时接口不能失败。
- 持仓列表固定高度并支持滚动；持仓列固定左侧，操作列固定右侧，市值、持有收益、今日收益 / 收益率、昨日收益、最新净值 / 当前价、价格日期右对齐且不换行；昨日收益必须独立成列，便于和今日收益口径分开查看；股票、ETF 和虚拟货币等实时价资产的今日收益按分段口径计算，上一交易日已有仓位按昨收价，新买仓位按成交价，新卖仓位按卖出价，手续费进入累计成本和账户扣款但不重复扣减当日涨跌；未清仓持仓不允许删除，只有清仓后才显示删除入口。
- 前端不得使用格式化后的当前价反算市值，市值、浮动盈亏和收益率以 `/api/holdings` 返回字段为准。

---

## 9. 统计分析页

### 页面目标

集中展示财务图表。

### 图表布局

```text
顶部：时间范围选择

第一行：
- 净资产趋势图
- 收支趋势图

第二行：
- 分类支出饼图
- 资产分布图

第三行：
- 投资盈亏柱状图
- 预算使用进度
```

---

## 10. AI 报告页

### 页面目标

查看每日、每周、每月财务报告。

### 页面布局

```text
顶部：报告类型切换：日报 / 周报 / 月报
左侧：报告列表
右侧：报告详情
```

### 报告详情结构

```text
标题
时间范围
净资产变化
收入支出摘要
投资盈亏摘要
异常提醒
AI 总结
```

---

## 11. 预算管理页

### 页面目标

设置和查看预算使用情况。

### 页面布局

```text
顶部：设置月预算
主体：分类预算卡片
底部：预算趋势图
```

---

## 12. 资产目标页

### 页面目标

设置长期资产目标并追踪进度。

### 页面结构

```text
目标金额
当前净资产
完成进度
剩余金额
预计达成时间
每月需要增长金额
```

---

# 三、开发文档

## 1. 技术架构

## 1.1 后端技术栈

| 技术 | 说明 |
|---|---|
| Java 17 | 后端语言 |
| Spring Boot 3.x | Web 框架 |
| MyBatis-Plus | ORM |
| MySQL 8 | 数据库 |
| Redis | 缓存，可选 |
| JWT | 登录认证 |
| XXL-JOB | 可视化定时任务调度中心 |
| Lombok | 简化代码 |
| Knife4j / Swagger | 接口文档 |

---

## 1.2 前端技术栈

| 技术 | 说明 |
|---|---|
| Vue 3 | 前端框架 |
| Vite | 构建工具 |
| TypeScript | 类型约束 |
| Element Plus | UI 组件库 |
| ECharts | 图表库 |
| Pinia | 状态管理 |
| Axios | HTTP 请求 |
| Vue Router | 路由 |

---

## 2. 系统架构

```text
前端 Vue3
   ↓
后端 Spring Boot API
   ↓
业务服务层 Service
   ↓
MyBatis Mapper
   ↓
MySQL

定时任务：
XXL-JOB Admin -> XOAssets Executor Handler -> 行情同步 / 价格聚合 / 投资快照 / 资产快照 / 汇率刷新
```

---

## 3. 后端模块划分

```text
com.XOAssets
├── common          通用模块
├── config          配置
├── security        登录认证
├── user            用户模块
├── account         账户模块
├── transaction     流水模块
├── category        分类模块
├── holding         投资持仓模块
├── price           行情价格模块
├── summary         每日汇总模块
├── budget          预算模块
├── report          AI 报告模块
├── goal            资产目标模块
└── job             定时任务
```

---

## 4. 数据库设计

## 4.1 用户表 user

```sql
CREATE TABLE `user` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `username` VARCHAR(50) NOT NULL COMMENT '用户名',
  `password` VARCHAR(255) NOT NULL COMMENT '密码',
  `nickname` VARCHAR(50) DEFAULT NULL COMMENT '昵称',
  `email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1正常 0禁用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

---

## 4.2 账户表 account

```sql
CREATE TABLE `account` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `account_name` VARCHAR(100) NOT NULL COMMENT '账户名称',
  `account_type` VARCHAR(30) NOT NULL COMMENT '账户类型：cash/bank/alipay/wechat/credit_card/securities/fund/crypto',
  `balance` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '账户余额',
  `currency` VARCHAR(10) NOT NULL DEFAULT 'CNY' COMMENT '币种',
  `include_in_assets` TINYINT NOT NULL DEFAULT 1 COMMENT '是否计入总资产：1是 0否',
  `sort_no` INT NOT NULL DEFAULT 0 COMMENT '排序',
  `remark` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='账户表';
```

---

## 4.3 分类表 category

```sql
CREATE TABLE `category` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `user_id` BIGINT DEFAULT NULL COMMENT '用户ID，NULL表示系统默认分类',
  `category_name` VARCHAR(50) NOT NULL COMMENT '分类名称',
  `category_type` VARCHAR(20) NOT NULL COMMENT '分类类型：income/expense',
  `parent_id` BIGINT DEFAULT NULL COMMENT '父分类ID',
  `icon` VARCHAR(50) DEFAULT NULL COMMENT '图标',
  `sort_no` INT NOT NULL DEFAULT 0 COMMENT '排序',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_type` (`user_id`, `category_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分类表';
```

---

## 4.4 交易流水表 transaction_record

```sql
CREATE TABLE `transaction_record` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `account_id` BIGINT DEFAULT NULL COMMENT '账户ID',
  `target_account_id` BIGINT DEFAULT NULL COMMENT '目标账户ID，转账时使用',
  `category_id` BIGINT DEFAULT NULL COMMENT '分类ID',
  `transaction_type` VARCHAR(30) NOT NULL COMMENT '交易类型：income/expense/transfer/refund/invest_buy/invest_sell',
  `amount` DECIMAL(18, 4) NOT NULL COMMENT '金额',
  `transaction_time` DATETIME NOT NULL COMMENT '交易时间',
  `remark` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `image_url` MEDIUMTEXT DEFAULT NULL COMMENT '流水图片，第一版保存上传后的图片地址或Data URL',
  `source_text` VARCHAR(255) DEFAULT NULL COMMENT '快捷记账原始文本',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_time` (`user_id`, `transaction_time`),
  KEY `idx_user_type_time` (`user_id`, `transaction_type`, `transaction_time`),
  KEY `idx_user_category_time` (`user_id`, `category_id`, `transaction_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='交易流水表';
```

---

## 4.5 公共资产表 xo_asset

```sql
CREATE TABLE `xo_asset` (
  `id` BIGINT NOT NULL COMMENT '资产ID',
  `symbol` VARCHAR(80) NOT NULL COMMENT '资产代码',
  `name` VARCHAR(120) NOT NULL COMMENT '资产名称',
  `type` VARCHAR(20) NOT NULL COMMENT '资产类型：STOCK FUND CRYPTO OTHER',
  `market` VARCHAR(30) NOT NULL DEFAULT 'UNKNOWN' COMMENT '交易市场：SH SZ BJ US CN_FUND CRYPTO UNKNOWN',
  `currency` VARCHAR(10) NOT NULL DEFAULT 'CNY' COMMENT '计价币种',
  `quote_source` VARCHAR(30) NOT NULL DEFAULT 'MANUAL' COMMENT '行情来源',
  `quote_key` VARCHAR(120) DEFAULT NULL COMMENT '外部行情查询键',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  KEY `idx_type_symbol` (`type`, `symbol`),
  KEY `idx_type_market_symbol` (`type`, `market`, `symbol`),
  UNIQUE KEY `uk_type_market_symbol_deleted` (`type`, `market`, `symbol`, `deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='公共资产基础表';
```

---

## 4.6 用户投资持仓表 xo_holding

```sql
CREATE TABLE `xo_holding` (
  `id` BIGINT NOT NULL COMMENT '持仓ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `asset_id` BIGINT NOT NULL COMMENT '资产ID',
  `quantity` DECIMAL(28, 10) NOT NULL DEFAULT 0 COMMENT '持仓数量，虚拟货币最多保留十位小数',
  `avg_cost` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '移动平均成本单价',
  `total_cost` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '持仓总成本',
  `remark` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1正常 0停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  KEY `idx_user_asset` (`user_id`, `asset_id`),
  KEY `idx_user_status` (`user_id`, `status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户投资持仓表';
```

---

## 4.7 投资交易流水表 xo_investment_transaction

```sql
CREATE TABLE `xo_investment_transaction` (
  `id` BIGINT NOT NULL COMMENT '投资交易ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `holding_id` BIGINT NOT NULL COMMENT '持仓ID',
  `asset_id` BIGINT NOT NULL COMMENT '资产ID',
  `account_id` BIGINT NOT NULL COMMENT '资金账户ID',
  `type` VARCHAR(20) NOT NULL COMMENT '交易类型：BUY SELL',
  `input_mode` VARCHAR(30) NOT NULL DEFAULT 'QUANTITY_PRICE' COMMENT '录入模式：QUANTITY_PRICE数量价格 AMOUNT_NAV金额净值',
  `trade_amount` DECIMAL(18, 4) DEFAULT NULL COMMENT '用户录入交易总金额',
  `trade_quantity` DECIMAL(28, 10) DEFAULT NULL COMMENT '用户录入交易数量',
  `trade_price` DECIMAL(18, 6) DEFAULT NULL COMMENT '用户录入成交价或净值',
  `quantity` DECIMAL(28, 10) NOT NULL COMMENT '交易数量，虚拟货币最多保留十位小数',
  `price` DECIMAL(18, 4) NOT NULL COMMENT '成交单价',
  `amount` DECIMAL(18, 4) NOT NULL COMMENT '成交金额',
  `fee` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '手续费',
  `cost_amount` DECIMAL(18, 4) DEFAULT NULL COMMENT '本次交易对应成本金额',
  `realized_profit` DECIMAL(18, 4) DEFAULT NULL COMMENT '已实现盈亏',
  `trade_date` DATE DEFAULT NULL COMMENT '交易发生日期',
  `confirmed_date` DATE DEFAULT NULL COMMENT '基金确认日期',
  `confirmed_nav` DECIMAL(18, 6) DEFAULT NULL COMMENT '基金确认单位净值',
  `confirmed_quantity` DECIMAL(28, 10) DEFAULT NULL COMMENT '基金确认份额',
  `status` VARCHAR(20) NOT NULL DEFAULT 'NORMAL' COMMENT '状态：NORMAL正常 CONFIRMED已确认 PENDING_CONFIRM待确认 FAILED失败 CANCELLED取消 REVOKED已撤销',
  `revoke_time` DATETIME DEFAULT NULL COMMENT '撤销时间',
  `revoke_reason` VARCHAR(255) DEFAULT NULL COMMENT '撤销原因',
  `transaction_time` DATETIME NOT NULL COMMENT '交易时间',
  `note` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  KEY `idx_user_time` (`user_id`, `transaction_time`),
  KEY `idx_user_holding_time` (`user_id`, `holding_id`, `transaction_time`),
  KEY `idx_user_account_time` (`user_id`, `account_id`, `transaction_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='投资交易流水表';
```

---

## 4.8 资产当前价格表 xo_asset_price_current

`xo_asset_price_current` 每个资产只保留一条最新可估值价格，用于持仓列表、首页投资资产和手动刷新后的即时展示。

```sql
CREATE TABLE `xo_asset_price_current` (
  `asset_id` BIGINT NOT NULL COMMENT '资产ID',
  `price` DECIMAL(28, 8) NOT NULL COMMENT '最新价格',
  `currency` VARCHAR(10) NOT NULL DEFAULT 'CNY' COMMENT '币种',
  `previous_close` DECIMAL(28, 8) DEFAULT NULL COMMENT '昨收价或上一交易日价格',
  `change_amount` DECIMAL(28, 8) DEFAULT NULL COMMENT '涨跌额',
  `change_percent` DECIMAL(18, 4) DEFAULT NULL COMMENT '涨跌幅百分比',
  `source` VARCHAR(30) NOT NULL COMMENT '行情来源',
  `quote_time` DATETIME NOT NULL COMMENT '报价时间',
  `market_status` VARCHAR(30) DEFAULT NULL COMMENT '市场状态',
  `raw_json` TEXT DEFAULT NULL COMMENT '行情原文',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`asset_id`),
  KEY `idx_quote_time` (`quote_time`),
  KEY `idx_source_time` (`source`, `quote_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='资产当前价格表';
```

---

## 4.8.1 资产日级价格表 xo_asset_price_daily

`xo_asset_price_daily` 是长期日级价格权威表。股票和虚拟货币由 Redis intraday 原始快照聚合 open / close / high / low；基金净值和手动价按单点日价写入，open / high / low / close 相同。

旧价格快照表已退役；历史库如仍存在旧表，先执行 `xoassets-server/src/main/resources/db/migration-retire-asset-price.sql` 迁移历史价，再删除旧表。

```sql
CREATE TABLE `xo_asset_price_daily` (
  `id` BIGINT NOT NULL COMMENT 'ID',
  `asset_id` BIGINT NOT NULL COMMENT '资产ID',
  `trade_date` DATE NOT NULL COMMENT '交易日期',
  `open_price` DECIMAL(28, 8) DEFAULT NULL COMMENT '开盘/首条价格',
  `close_price` DECIMAL(28, 8) NOT NULL COMMENT '收盘/最后价格',
  `high_price` DECIMAL(28, 8) DEFAULT NULL COMMENT '最高价',
  `low_price` DECIMAL(28, 8) DEFAULT NULL COMMENT '最低价',
  `previous_close` DECIMAL(28, 8) DEFAULT NULL COMMENT '上一交易日收盘价',
  `change_amount` DECIMAL(28, 8) DEFAULT NULL COMMENT '较上一交易日涨跌额',
  `change_percent` DECIMAL(18, 4) DEFAULT NULL COMMENT '较上一交易日涨跌幅',
  `currency` VARCHAR(10) NOT NULL DEFAULT 'CNY' COMMENT '币种',
  `source` VARCHAR(30) NOT NULL COMMENT '行情来源',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_asset_trade_date` (`asset_id`, `trade_date`, `deleted`),
  KEY `idx_asset_date` (`asset_id`, `trade_date`),
  KEY `idx_trade_date` (`trade_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='资产日级价格表';
```

---

## 4.8.2 市场交易日历表 xo_market_calendar

```sql
CREATE TABLE `xo_market_calendar` (
  `id` BIGINT NOT NULL COMMENT '日历ID',
  `market` VARCHAR(30) NOT NULL COMMENT '市场：A_SHARE 港股/美股后续扩展',
  `trade_date` DATE NOT NULL COMMENT '日期',
  `trading_day` TINYINT(1) NOT NULL COMMENT '是否交易日：1是 0否',
  `source` VARCHAR(50) NOT NULL DEFAULT 'SYSTEM_WEEKDAY' COMMENT '来源：SYSTEM_WEEKDAY EXCHANGE_ANNOUNCEMENT MANUAL',
  `remark` VARCHAR(255) DEFAULT NULL COMMENT '备注',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_market_date` (`market`, `trade_date`, `deleted`),
  KEY `idx_market_trading_day` (`market`, `trading_day`, `trade_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='市场交易日历表';
```

---

## 4.9 资产快照表 xo_asset_snapshot

```sql
CREATE TABLE `xo_asset_snapshot` (
  `id` BIGINT NOT NULL COMMENT '资产快照ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `snapshot_date` DATE NOT NULL COMMENT '快照日期',
  `cash_asset` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '现金资产，正余额账户合计',
  `investment_asset` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '投资资产市值',
  `total_asset` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '总资产',
  `liability` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '负债，负余额账户绝对值合计',
  `net_asset` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '净资产',
  `investment_cost` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '投资持仓成本',
  `investment_profit` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '投资浮动盈亏',
  `investment_profit_rate` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '投资收益率百分比',
  `monthly_income` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '截至快照日的当月普通收入',
  `monthly_expense` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '截至快照日的当月普通支出',
  `monthly_balance` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '截至快照日的当月结余',
  `budget_used_amount` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '截至快照日的预算已使用金额',
  `budget_total_amount` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '预算总金额',
  `budget_usage_rate` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '预算使用率百分比',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_snapshot_date` (`user_id`, `snapshot_date`, `deleted`),
  KEY `idx_user_date` (`user_id`, `snapshot_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户资产快照表';
```

---

## 4.8 AI 报告表 xo_ai_report

```sql
CREATE TABLE `xo_ai_report` (
  `id` BIGINT NOT NULL COMMENT '报告ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `report_type` VARCHAR(20) NOT NULL COMMENT '报告类型：DAILY WEEKLY MONTHLY',
  `report_date` DATE NOT NULL COMMENT '报告日期',
  `title` VARCHAR(120) NOT NULL COMMENT '报告标题',
  `content` TEXT NOT NULL COMMENT '报告内容',
  `summary_json` TEXT DEFAULT NULL COMMENT '结构化摘要',
  `status` VARCHAR(20) NOT NULL DEFAULT 'SUCCESS' COMMENT '状态：SUCCESS FAILED',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  KEY `idx_user_date` (`user_id`, `report_date`),
  KEY `idx_user_type_date` (`user_id`, `report_type`, `report_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI报告表';
```

---

## 4.9 预算表 xo_budget

```sql
CREATE TABLE `xo_budget` (
  `id` BIGINT NOT NULL COMMENT '预算ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `month` VARCHAR(7) NOT NULL COMMENT '预算月份，格式 yyyy-MM',
  `category_id` BIGINT DEFAULT NULL COMMENT '分类ID，总预算为空',
  `budget_type` VARCHAR(20) NOT NULL COMMENT '预算类型：TOTAL CATEGORY',
  `amount` DECIMAL(18, 4) NOT NULL COMMENT '预算金额',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1启用 0停用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  KEY `idx_user_month` (`user_id`, `month`),
  KEY `idx_user_category_month` (`user_id`, `category_id`, `month`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='预算表';
```

---

## 4.10 资产目标表 xo_goal

```sql
CREATE TABLE `xo_goal` (
  `id` BIGINT NOT NULL COMMENT '目标ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `name` VARCHAR(100) NOT NULL COMMENT '目标名称',
  `target_amount` DECIMAL(18, 4) NOT NULL COMMENT '目标金额',
  `current_amount` DECIMAL(18, 4) NOT NULL DEFAULT 0 COMMENT '当前金额',
  `target_date` DATE DEFAULT NULL COMMENT '目标日期',
  `status` VARCHAR(20) NOT NULL DEFAULT 'ACTIVE' COMMENT '状态：ACTIVE DONE',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT NOT NULL DEFAULT 0 COMMENT '逻辑删除：0否 1是',
  PRIMARY KEY (`id`),
  KEY `idx_user_status` (`user_id`, `status`),
  KEY `idx_user_target_date` (`user_id`, `target_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='资产目标表';
```

---

## 5. 核心接口设计

## 5.1 用户接口

| 方法 | 地址 | 说明 |
|---|---|---|
| POST | /api/auth/register | 注册 |
| POST | /api/auth/login | 登录 |
| POST | /api/auth/logout | 退出 |
| GET | /api/user/profile | 当前用户信息 |
| PUT | /api/user/profile | 修改用户信息 |

---

## 5.2 首页接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/dashboard/overview | 首页概览 |
| GET | /api/dashboard/net-assets-trend | 净资产趋势 |
| GET | /api/dashboard/asset-distribution | 资产分布 |
| GET | /api/dashboard/recent-transactions | 最近流水 |

---

## 5.3 流水接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/transactions | 分页查询流水 |
| POST | /api/transactions | 新增流水 |
| PUT | /api/transactions/{id} | 修改流水 |
| DELETE | /api/transactions/{id} | 删除流水 |
| POST | /api/transactions/quick-parse | 快捷记账解析 |

---

## 5.4 账户接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/accounts | 查询账户列表 |
| POST | /api/accounts | 新增账户 |
| PUT | /api/accounts/{id} | 修改账户 |
| DELETE | /api/accounts/{id} | 删除账户 |
| POST | /api/accounts/{id}/balance-adjustments | 生成账户余额修正记录 |
| GET | /api/accounts/{id}/balance-trend | 查询账户日终余额曲线 |

---

## 5.5 投资持仓接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/assets/search | 搜索公共资产 |
| GET | /api/assets/lookup | 按类型和代码 / 名称识别资产信息 |
| POST | /api/assets | 新增公共资产 |
| GET | /api/holdings | 查询持仓列表 |
| GET | /api/holdings/summary | 查询持仓汇总 |
| GET | /api/holdings/{id}/detail | 查询单个持仓详情 |
| POST | /api/holdings | 新增持仓 |
| PUT | /api/holdings/{id} | 修改持仓 |
| DELETE | /api/holdings/{id} | 删除持仓 |
| GET | /api/investments/overview | 查询投资总览，今日收益按今日有效价格动态汇总 |
| GET | /api/investments/holdings | 按 ALL / FUND / STOCK / CRYPTO 查询模块持仓 |
| GET | /api/investments/trend | 查询总览或模块资产趋势，总览点位返回按全持仓收益日历聚合并折算人民币后的 `dailyProfit` / `dailyProfitRate` |
| GET | /api/investments/daily-profit | 查询全持仓每日收益，按持仓收益日历同日汇总后统一折算人民币 |
| GET | /api/investments/holdings/{id}/profit-calendar | 查询单持仓月度收益日历 |
| POST | /api/investment-transactions | 买入 / 卖出 |
| GET | /api/investment-transactions | 查询投资交易 |
| GET | /api/investment-transactions/fund-confirm-preview | 预估基金申请日 / 确认日 |
| PUT | /api/investment-transactions/{id}/revoke | 撤销投资交易 |
| POST | /api/quotes/manual | 手动更新价格 |
| POST | /api/quotes/refresh | 根据资产配置刷新行情 |
| POST | /api/quotes/refresh-batch | 批量刷新持仓行情 |
| GET | /api/exchange-rates/usd-cny | 查询 USD/CNY 展示汇率 |

接口使用规则：

- 前端投资页不再直接调用 `/api/assets/**`，创建或编辑持仓时由 `/api/holdings` 请求携带资产名称、代码、类型、币种和行情源。
- 前端新增持仓时可先调用 `GET /api/assets/lookup`，查询失败时展示“资产信息查询失败，可手动录入”，且不阻塞手动保存。
- 后端收到持仓请求后自动创建或复用内部 `xo_asset`。
- 如果持仓请求带有 `latestPrice`、`previousClose`、`changePercent`、`quoteTime`，后端在创建持仓时写入 `xo_asset_price_current`，带报价日期的初始价同步写入 `xo_asset_price_daily`。
- 首页 `GET /api/dashboard/overview` 的趋势率缺少上期基准时返回 `null`，Web 展示 `--`，不能用 0 冒充缺失趋势。
- `POST /api/investment-transactions` 必须传 `accountId`；后端按当前登录用户校验账户、持仓和交易归属。
- `GET /api/holdings` 返回最新价、昨价、前日价、今日收益、昨日收益、浮动盈亏、收益率、回本涨幅、价格日期、今日价格可用性和价格状态；所有资产只有当前价格日期等于今天时才计算今日收益，其中基金和股票还必须当天为交易日，非交易日返回 `priceStatus=MARKET_CLOSED` 并展示休市，交易日未更新或虚拟货币当前价过期返回 `priceStatus=TODAY_PRICE_NOT_AVAILABLE`；收益基准价格或基准持仓数量缺失时对应字段返回 `null`，前端展示 `--`；投资总览和模块卡还要返回 `todayProfitStatusLabel` / `primaryProfitStatusLabel`，让今日收益不可用时能明确提示“今日休市”或净值未更新。
- 今日收益同时返回 `todayProfit`（前端默认展示主口径）、`todayProfitByCurrentQuantity`（当前 / 今日有效份额口径）和 `todayProfitByPreviousSnapshotQuantity`（上一交易日日终份额归因口径）；股票、ETF、虚拟货币等实时价资产的 `todayProfit` 使用日内分段收益，避免当日新增仓位被错误按昨收价计算。昨日收益取单持仓收益日历中今天之前最近一个有收益日，聚合时按同一天所有持仓收益日历 `profitAmount` 汇总，并在基金、股票、虚拟货币持仓列表中作为独立列展示；持仓详情页今日收益卡片的辅助区展示昨日收益，避免把净值未更新提示挤占收益对比位置；持仓汇总、投资总览和模块卡通过 `todayProfitAvailable` / `primaryProfitAvailable` 标记今日收益是否可展示，不可用时金额返回 `null` 并显示 `--`；投资总今日 / 本月收益按 `当前市值 - 基准市值 - 净入金` 计算。
- 投资日快照按 `xo_investment_transaction` 截至快照日重建历史持仓、成本、市值、已实现收益和当日投资本金净流入，不得用当前 `xo_holding` 回填历史；`daily_profit` 是快照日资金流调整收益，公式为本日投资市值 - 上一快照日投资市值 - 当日投资本金净流入，不等同于收益日历展示日收益；基金金额买入从实际申购日至确认日前按在途投资资产计入，即使交易后来已确认，补跑确认日前历史快照也必须保留这段在途金额，但在途存量不能每天重复计入 `net_inflow`；补跑历史快照时按 `trade_date` 使用已回填的 `xo_asset_price_daily` 日级价格，不以价格行 `created_at` 限制历史修正。
- `GET /api/holdings/{id}/detail` 必须按当前登录用户校验持仓归属，返回持仓估值、正常交易汇总、该持仓全部交易记录、最近 30 条价格点和收益日历；Web 端对交易记录做本地分页展示，默认 10 条；持仓详情市值趋势要按每个价格日期重建该持仓历史份额和成本，不能用当前份额倒推历史；基金和股票收益日历中周末 / 节假日必须返回 `marketClosed=true`、`statusLabel=休市`，不能和普通缺价格混淆。
- 撤销投资交易不物理删除记录；撤销 BUY 加回账户余额并减少持仓数量和成本，撤销 SELL 扣回到账金额并按 `cost_amount` 恢复持仓成本。

## 5.5.1 账户资金中心与导出接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/accounts/{id}/ledger | 查询账户资金明细 |
| GET | /api/accounts/{id}/flow-statistics | 查询账户详情统计（资金汇总、支出分类、余额曲线） |
| POST | /api/accounts/{id}/balance-adjustments | 生成账户余额修正记录 |
| GET | /api/accounts/{id}/balance-trend | 查询账户日终余额曲线 |
| GET | /api/export/account-ledger | 导出账户资金明细 CSV |
| GET | /api/export/transactions | 导出普通流水 CSV |
| GET | /api/export/investment-transactions | 导出投资交易 CSV |

账户资金明细规则：

- 普通流水来自 `xo_transaction`，投资交易来自 `xo_investment_transaction`，余额修正来自 `xo_account_balance_adjustment`。
- 账户详情只做聚合查询，不把投资买入 / 卖出写入普通流水。
- 余额修正生成专用调整事件，不计入普通收入 / 支出统计，但进入账户资金明细、流向统计的 `adjustmentAmount` 和账户日终余额曲线。
- CSV 导出只能导出当前登录用户自己的数据，使用 UTF-8 BOM，金额保留 4 位小数。

行情刷新说明：

- 后端通过 `QuoteProvider` 抽象不同行情来源，前端不得直接请求第三方行情接口。
- 手动行情由 `ManualQuoteProvider` 兜底提示，不自动刷新。
- CoinGecko 行情由 `CoinGeckoQuoteProvider` 处理，支持 CRYPTO 资产的 BTC、ETH、SOL、BNB、DOGE，使用资产 `quote_key` 查询价格，支持 CoinGecko id 和常见币种简写。
- 基金净值由 `EastMoneyFundQuoteProvider` 处理，`quote_key` 使用基金代码，优先读取天天基金 F10 历史净值表，当前价使用最新单位净值，昨价使用上一交易日单位净值。
- 股票行情由 `StockQuoteProvider` 处理，A 股使用新浪行情，`quote_key` 使用 `600519.SH` / `000001.SZ` / `430047.BJ`；美股使用 Yahoo Finance，`quote_key` 使用 `AAPL` 等代码。
- 刷新成功写入 `xo_asset_price_current`；基金和手动价同步写入 `xo_asset_price_daily`，股票和虚拟货币由 Redis 原始快照聚合到日级价格表。刷新失败保留最近 current，完全没有 current 时返回错误并引导用户手动录入价格。
- 最近价格由 `QuoteService.latestPriceMap` 统一查询 `xo_asset_price_current`，持仓列表优先使用当前价估值。
- 行情缓存 TTL：CRYPTO 15 分钟、STOCK 15 分钟、FUND 1 天、MANUAL 不自动过期；Redis 原始行情快照只保存股票和虚拟货币，按 15 分钟写入，TTL 3 天；日级聚合读取某天原始快照时按 ZSET score 的当天起止毫秒范围查询，不全量读取整月数据后筛选；基金净值不写 Redis，直接写当前价和日级价格表。
- 股票只在交易日 09:30-15:30 之间拉取第三方行情；虚拟货币全天刷新；基金在 19:30-23:30 每半小时强制尝试刷新净值，包含 0 份额待确认基金持仓。
- USD/CNY 汇率按天缓存，MVP 使用进程内缓存，后续可替换 Redis；汇率刷新失败时前端继续使用最近缓存或默认值。
- 行情刷新由 XXL-JOB handler `refreshMarketQuotes`、`refreshFundQuotes`、`refreshFundQuotesFollowup` 按资产类型分开触发，任务失败或单个资产失败只记录日志，不影响主应用启动。

---

## 5.6 图表接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/analytics/income-expense-trend | 收支趋势 |
| GET | /api/analytics/expense-category | 支出分类 |
| GET | /api/analytics/investment-profit | 投资盈亏 |
| GET | /api/analytics/net-assets | 净资产趋势 |
| GET | /api/analytics/budget-progress | 预算进度 |
| GET | /api/statistics/net-assets-trend | 净资产趋势 |
| GET | /api/statistics/income-expense-trend | 收支趋势 |
| GET | /api/statistics/expense-category | 分类支出占比 |
| GET | /api/statistics/asset-distribution | 资产分布 |
| GET | /api/statistics/investment-profit-trend | 投资盈亏趋势 |
| GET | /api/statistics/budget-progress | 预算使用进度 |
| GET | /api/snapshots/latest | 最新资产快照 |
| GET | /api/snapshots/trend | 资产快照趋势 |
| POST | /api/snapshots/generate-today | 手动生成或更新今日快照 |
| POST | /api/snapshots/generate | 手动生成或更新指定日期快照 |
| POST | /api/snapshots/rebuild | 手动重建当前用户指定日期区间资产快照 |

统计口径：

- 快照总资产 = 现金资产 + 投资资产。
- 快照净资产 = 总资产 - 负债。
- 现金资产 = 正余额账户合计；负债 = 负余额账户绝对值合计。
- 投资市值 = 持仓数量 * 最近价格；没有价格时按平均成本兜底。
- 最新快照较昨日 / 较月初缺少有效基准时返回 `null`，前端展示 `--`；本月只有最新一条且最新日期不是 1 号时，较月初也不能显示 0。
- 支出统计排除转账，退款抵扣支出。
- 首页和统计接口都按当前登录 `user_id` 隔离；首页净资产变化和数据分析资产趋势优先使用 `xo_asset_snapshot`。
- 首页净资产卡不复用当月结余环比作为趋势率；净资产较昨日变化使用资产快照金额单独展示，缺少基准时显示 `--`。
- 资产快照生成 `xo_asset_snapshot` 时，现金资产必须按账户初始余额 + 快照日前普通流水 / 投资交易 / 余额修正重建历史余额，不能用当前 `xo_account.balance` 回填历史；投资资产必须按快照日通过交易流水重建历史头寸，再使用 `xo_asset_price_daily.trade_date <= snapshotDate` 的同币种日级价或不晚于快照日的当前价估值；确认后的基金金额买入在确认日前补跑历史快照时仍按在途投资资产计入，不能用当前 `xo_holding` 数量或当前确认状态倒推历史。
- 普通流水补录、修改或删除后必须从受影响日期起重建资产快照；投资交易补录、基金确认或撤销后必须从交易日起先重建投资日快照，再重建资产快照；31 天内同步重建，超过 31 天合并写入 `xo_snapshot_rebuild_task`，由 `rebuildPendingAssetSnapshots` 定时处理，避免批量导入或长跨度补录阻塞业务接口。
- 本地对账可调用 `POST /api/investments/snapshots/generate?snapshotDate=yyyy-MM-dd` 重建当前用户投资日快照，再调用 `POST /api/snapshots/generate?snapshotDate=yyyy-MM-dd` 重建当前用户单日资产快照；批量普通流水补录后可调用 `POST /api/snapshots/rebuild?startDate=yyyy-MM-dd&endDate=yyyy-MM-dd` 重建区间资产快照，投资交易补录由交易服务自动请求投资日快照和资产快照重建。

---

## 5.7 预算接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/budgets | 查询月度预算列表 |
| POST | /api/budgets | 新增总预算或分类预算 |
| PUT | /api/budgets/{id} | 修改预算 |
| DELETE | /api/budgets/{id} | 删除预算 |
| GET | /api/budgets/summary | 查询预算汇总 |

预算规则：

- 每个用户每个月只能有一个 `TOTAL` 总预算。
- 每个用户每个月每个支出分类只能有一个 `CATEGORY` 分类预算。
- 预算使用额从 `xo_transaction` 汇总，`TRANSFER` 不计入，`REFUND` 抵扣支出。
- 使用率状态：`NORMAL` 低于 80%，`WARNING` 为 80% 到 100%，`OVER` 为超过 100%。

---

## 5.8 AI 报告接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/reports | 查询报告列表 |
| GET | /api/reports/{id} | 查询报告详情 |
| POST | /api/reports/generate-preview | 生成模板化报告 |

阶段七规则：

- 只基于当前用户首页、预算、投资等真实统计数据生成模板化复盘。
- 不调用真实 AI，不组装外部大模型请求。
- 报告只做数据总结与风险提示，不提供投资买入 / 卖出建议。
- 报告查询和详情必须按当前 `user_id` 隔离。

---

## 5.9 资产目标接口

| 方法 | 地址 | 说明 |
|---|---|---|
| GET | /api/goals | 查询当前用户目标 |
| POST | /api/goals | 新增目标 |
| PUT | /api/goals/{id} | 修改目标 |
| DELETE | /api/goals/{id} | 删除目标 |
| GET | /api/goals/summary | 查询目标汇总 |

目标规则：

- 当前金额可以手动填写，也可以选择使用当前净资产。
- 返回完成率、剩余金额、距离目标日期剩余天数、预计每月需要增长金额。
- 目标查询、修改、删除都按当前登录 `user_id` 隔离。

---

## 6. 核心业务流程

## 6.1 新增支出流程

```text
用户新增支出
  ↓
校验金额、账户、分类
  ↓
保存 transaction_record
  ↓
扣减 account.balance
  ↓
用户可在首页右上角手动生成当日资产快照
  ↓
返回成功
```

事务边界：

- 保存流水
- 更新账户余额
- 刷新汇总

必须在同一个事务中完成。

---

## 6.2 新增收入流程

```text
用户新增收入
  ↓
保存收入流水
  ↓
增加账户余额
  ↓
刷新每日汇总
  ↓
返回成功
```

---

## 6.3 转账流程

```text
用户发起转账
  ↓
校验转出账户和转入账户
  ↓
保存转账流水
  ↓
转出账户扣款
  ↓
转入账户加款
  ↓
刷新每日汇总
```

注意：

- 转账不计入收入支出。
- 转账只影响账户余额，不影响净资产。

---

## 6.4 投资买入流程

```text
用户买入资产
  ↓
校验资金账户归属和账户余额
  ↓
扣减资金账户余额
  ↓
保存投资买入流水
  ↓
增加或更新持仓数量
  ↓
重新计算平均成本
  ↓
不写入普通收支流水
```

平均成本计算：

```text
新总成本 = 原总成本 + 买入数量 × 买入价格 + 手续费
新成本价 = 新总成本 / (原数量 + 买入数量)
```

基金金额买入确认：

```text
用户选择基金买入
  ↓
录入买入总金额、手续费和实际买入时间
  ↓
系统按数据库市场日历计算有效申请日和确认日
  ↓
交易日 15:00:00 及以前按当天申请；15:00 后、周末或休市日顺延到下一交易日
  ↓
普通基金 T+1 确认；名称包含 QDII 的基金 T+2 确认
  ↓
系统按确认日查询基金单位净值
  ↓
查到净值：确认份额 = (买入总金额 - 手续费) / 确认净值，保留 10 位
  ↓
持仓总成本增加买入总金额，成本价 = 新总成本 / 新确认份额合计
  ↓
查不到净值：交易保存为 PENDING_CONFIRM，资金账户已扣款但不增加持仓份额
  ↓
定时任务后续查到单位净值后，在同一事务内确认交易并更新持仓
  ↓
从实际买入日起请求投资日快照和资产快照重建；31 天内同步处理，长跨度合并到 `xo_snapshot_rebuild_task` 后台处理，避免确认后首页和投资总览基准滞后
```

基金字段口径：

```text
transactionTime = 用户实际买入时间
tradeDate = 用户实际买入日期
confirmedDate = 基金预计确认日期
confirmedNav = 确认单位净值
confirmedQuantity = 确认份额
costAmount = 买入总金额
```

---

## 6.5 投资卖出流程

```text
用户卖出资产
  ↓
校验资金账户归属和持仓数量
  ↓
计算卖出成本和已实现盈亏
  ↓
保存投资卖出流水
  ↓
减少持仓数量和总成本
  ↓
增加资金账户余额
  ↓
不写入普通收支流水
```

---

## 6.6 资产快照生成流程

```text
定时任务触发
  ↓
查询用户账户余额
  ↓
查询投资持仓市值
  ↓
查询截至快照日的当月普通收入、支出和预算使用
  ↓
计算现金资产、投资资产、负债、净资产
  ↓
同一用户同一天存在快照则更新
  ↓
保存 xo_asset_snapshot
```

---

## 6.7 AI 报告生成流程

```text
定时任务触发
  ↓
读取首页统计和预算汇总
  ↓
读取当日分类支出
  ↓
读取投资盈亏
  ↓
生成模板化复盘文本和结构化摘要
  ↓
保存 xo_ai_report
```

---

## 7. 关键代码结构建议

## 7.1 Controller 示例结构

```java
@RestController
@RequestMapping("/api/transactions")
@RequiredArgsConstructor
public class TransactionController {

    private final TransactionService transactionService;

    @GetMapping
    public PageResult<TransactionVO> page(TransactionQuery query) {
        return transactionService.page(query);
    }

    @PostMapping
    public Long create(@RequestBody TransactionCreateDTO dto) {
        return transactionService.create(dto);
    }

    @PutMapping("/{id}")
    public void update(@PathVariable Long id, @RequestBody TransactionUpdateDTO dto) {
        transactionService.update(id, dto);
    }

    @DeleteMapping("/{id}")
    public void delete(@PathVariable Long id) {
        transactionService.delete(id);
    }
}
```

---

## 7.2 Service 事务示例

```java
@Service
@RequiredArgsConstructor
public class TransactionServiceImpl implements TransactionService {

    private final TransactionRecordMapper transactionRecordMapper;
    private final AccountMapper accountMapper;
    private final DailySummaryService dailySummaryService;

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Long create(TransactionCreateDTO dto) {
        validateTransaction(dto);

        TransactionRecord record = buildRecord(dto);
        transactionRecordMapper.insert(record);

        updateAccountBalance(dto);

        dailySummaryService.refresh(dto.getUserId(), dto.getTransactionTime().toLocalDate());

        return record.getId();
    }
}
```

---

## 8. 数据统计口径

## 8.1 总资产

```text
总资产 = 现金类账户余额 + 投资资产市值
```

## 8.2 总负债

```text
总负债 = 信用卡负债 + 借款 + 贷款
```

## 8.3 净资产

```text
净资产 = 总资产 - 总负债
```

## 8.4 今日净资产变化

```text
今日净资产变化 = 今日净资产 - 昨日净资产
```

## 8.5 投资浮动盈亏

```text
浮动盈亏 = 当前市值 - 持仓成本
```

## 8.6 投资收益率

```text
收益率 = 浮动盈亏 / 持仓成本 × 100%
```

## 8.7 投资期间收益

```text
期间收益 = 当前投资市值 - 基准投资市值 - 期间净入金
```

- 买入为正向投资本金净流入，卖出为负向投资本金净流入；基金金额申购按实际下单扣款日计入，确认日只影响持仓份额生效。
- 今日收益、昨日收益和本月收益都必须剔除期间买卖带来的资金流入 / 流出。
- 持仓明细今日收益同时返回当前 / 今日有效份额口径和上一交易日日终份额归因口径，Web 默认展示当前份额口径。
- 每日收益日历、昨日收益、投资趋势每日收益和收益贡献统一从 `xo_investment_holding_daily_profit` 聚合；基金 / QDII 净值收益按净值日后的下一交易日展示，股票按价格日展示。
- `xo_investment_daily_snapshot.daily_profit` 表示快照日资金流调整收益，不等同于收益日历展示日收益；`calendar_profit` 表示持仓每日收益表按展示日聚合后的真实收益；`buy_amount` / `sell_amount` / `fee_amount` 按资金实际发生日统计，基金确认日只影响份额生效。

## 8.8 账户余额修正

- 余额修正只用于现实余额校准，生成 `xo_account_balance_adjustment` 专用事件。
- 余额修正不进入普通收入 / 支出统计；它进入账户账本、`adjustmentAmount` 和日终余额曲线。

---

## 9. 权限与安全

## 9.1 登录认证

- 使用 JWT。
- Token 放在 Authorization Header。
- 后端所有接口必须通过 userId 隔离数据。

## 9.2 数据隔离

所有业务表必须带 user_id。

查询、修改、删除都必须校验：

```text
当前登录用户 ID = 数据所属 user_id
```

## 9.3 密码安全

- 密码不能明文存储。
- 使用 BCrypt 加密。

## 9.4 金融数据安全

- 不在日志中打印金额明细、账户信息、Token。
- 后端通过 `logback-spring.xml` 按 `XOASSETS_PROFILE` 区分 dev / test / prod 日志级别；dev 可输出 SQL 调试日志，生产环境不得开启 SQL DEBUG。
- AI 请求前要尽量脱敏。
- 后续云同步时考虑字段级加密。

---

## 10. 异常场景

| 场景 | 处理 |
|---|---|
| 金额为空 | 提示金额必填 |
| 金额小于等于 0 | 提示金额必须大于 0 |
| 账户不存在 | 提示账户不存在 |
| 分类不存在 | 提示分类不存在 |
| 转账账户相同 | 不允许同账户转账 |
| 投资卖出数量超过持仓 | 提示持仓不足 |
| 删除流水后账户余额异常 | 删除时反向恢复账户余额 |
| 行情获取失败 | 使用最近一次价格，并标记价格过期 |
| AI 报告生成失败 | 标记失败，可手动重试 |

---

## 11. 定时任务设计

| 任务 | XXL-JOB Handler | 频率 | 说明 |
|---|---|---|---|
| 股票 / 虚拟货币行情同步 | `refreshMarketQuotes` | 每 15 分钟 | 股票仅交易日 09:30-15:30 拉取，虚拟货币全天拉取，Redis 原始快照 TTL 3 天 |
| 基金净值晚间刷新 | `refreshFundQuotes` / `refreshFundQuotesFollowup` | 19:30-23:30 每 30 分钟 | 强制尝试刷新基金净值，写入当前价和日级价格表 |
| 资产日级价格聚合 | `aggregateRecentAssetPrices` / `aggregateLateRecentAssetPrices` | 19:25-22:55 每 30 分钟，23:25 补一轮 | 只处理股票 / 虚拟货币，补跑最近 4 个自然日，从 Redis 聚合 open/close/high/low；基金不参与 Redis 聚合，由基金刷新直接写日级净值 |
| 投资资产日快照 | `snapshotRecentInvestmentDays` / `snapshotRecentInvestmentDaysFollowup` | 19:30-23:30 每 30 分钟 | 先重建持仓每日收益，再补跑最近 4 个自然日并 upsert 用户投资快照，确保周末后和基金净值延迟时可被后续覆盖 |
| 用户资产快照 | `generateDailySnapshots` | 每天 23:50，可手动生成 | upsert 当日用户资产快照，同一用户同一天只保留一条 |
| 待重建资产快照处理 | `rebuildPendingAssetSnapshots` | 每 10 分钟 | 处理普通流水和投资交易补录产生的 `xo_snapshot_rebuild_task`；投资触发任务先补投资日快照，再补资产快照 |
| 基金待确认扫描 | `confirmPendingFundTransactions` | 每 30 分钟 | 查询确认日净值并把 PENDING_CONFIRM 转为 CONFIRMED |
| 市场日历补齐 | `refreshYearlyMarketCalendar` | 每年 1 月 1 日 00:05 | 补齐当年和下一年基础周末日历，交易所休市日以数据库修正记录为准 |
| 每日汇总 | 当前无独立 handler | 每天 00:10 | 规划任务，当前代码未实现独立调度 |
| AI 日报 | 当前无独立 handler | 每天 00:20 | 规划任务，当前代码未实现独立调度 |
| 预算检查 | 当前无独立 handler | 每天 09:00 | 规划任务，当前代码未实现独立调度 |
| USD/CNY 汇率缓存刷新 | `refreshDailyUsdCnyExchangeRate` | 每天 06:20 | 刷新美元兑人民币日缓存，失败保留旧缓存 |

---

## 12. 第一阶段开发计划

## 12.1 Sprint 1：基础框架

- 搭建 Spring Boot 项目
- 搭建 Vue3 项目
- 登录注册
- JWT 鉴权
- 基础用户表
- 统一响应结构
- 全局异常处理

## 12.2 Sprint 2：记账核心

- 账户管理
- 分类管理
- 收支流水
- 转账功能
- 流水查询
- 首页最近流水

## 12.3 Sprint 3：资产看板

- 首页资产概览
- 净资产计算
- 每日汇总
- 支出趋势图
- 分类支出图

## 12.4 Sprint 4：投资持仓

- 持仓管理
- 买入卖出
- 成本计算
- 盈亏计算
- 投资图表

## 12.5 Sprint 5：AI 报告和预算

- AI 日报
- 报告列表
- 预算管理
- 预算预警
- 资产目标

## 12.6 Sprint 6：体验和稳定性

- 业务列表和图表无数据时显示空状态。
- 删除账户、分类、流水、持仓、预算、目标前必须二次确认。
- 金额、价格、数量等关键输入前端先拦截无效值，后端继续使用 Bean Validation 和业务校验兜底。
- 接口错误统一展示后端 `message`，避免吞掉业务失败原因。
- 前端业务请求展示 loading，后端关键余额、持仓、交易联动逻辑保留事务注释。
- 所有用户业务数据继续按当前登录 `user_id` 隔离。

---

## 13. MVP 功能边界

### 第一版必须做

- 登录注册
- 账户管理
- 分类管理
- 收支记账
- 转账
- 投资持仓手动维护
- 首页仪表盘
- 图表统计
- 每日汇总

### 第一版建议做

- AI 日报
- 预算管理
- 资产目标

### 第一版不建议做

- 银行卡自动同步
- 支付宝 / 微信自动同步
- 券商自动同步
- 投资买卖建议
- 社交功能
- 复杂后台管理

---

## 14. 后续扩展方向

| 方向 | 说明 |
|---|---|
| 移动端 App | Flutter 或 React Native |
| 小程序 | 轻量记账入口 |
| 账单导入 | 支持支付宝、微信账单 CSV 导入 |
| OCR 识别 | 拍照识别小票 |
| 多币种 | 当前投资聚合支持 CNY、USD；其他币种需补齐汇率后再开放 |
| 家庭账本 | 多用户共享账本 |
| 本地优先 | 数据默认本地存储，云同步可选 |
| RAG 财务助手 | 基于个人账本问答 |
| 订阅识别 | 自动识别会员、订阅扣费 |
| 消费异常检测 | 自动识别异常消费 |

---

# 四、前端页面原型草图

## 1. 首页仪表盘

```text
┌──────────────────────────────────────────────┐
│ 小〇财迹 XOAssets                    用户头像  │
├───────────────┬──────────────────────────────┤
│ 菜单           │ 总资产  ¥180,000             │
│ - 首页         │ 净资产  ¥152,360   +216.30   │
│ - 记账         │ 今日支出 ¥126.50             │
│ - 账户         │ 投资盈亏 +342.80             │
│ - 持仓         ├──────────────────────────────┤
│ - 统计         │ 净资产趋势图                 │
│ - 报告         ├──────────────┬───────────────┤
│ - 预算         │ 资产分布图    │ 今日AI总结    │
│ - 设置         ├──────────────┴───────────────┤
│                │ 最近流水                     │
└──────────────────────────────────────────────┘
```

---

## 2. 记账流水页

```text
┌──────────────────────────────────────────────┐
│ 快速记账：中午吃饭 32              新增流水 │
├──────────────────────────────────────────────┤
│ 时间筛选  类型筛选  分类筛选  账户筛选      │
├──────────────────────────────────────────────┤
│ 时间        类型   分类   账户     金额 操作 │
│ 05-28 12:30 支出   餐饮   支付宝   -32 编辑 │
│ 05-28 09:00 收入   工资   工资卡   +12000   │
└──────────────────────────────────────────────┘
```

---

## 3. 投资持仓页

```text
┌──────────────────────────────────────────────┐
│ 投资总市值 ¥80,000  总盈亏 +6,200  今日 +320 │
├──────────────────────────────────────────────┤
│ 类型筛选：全部 / 股票 / 基金 / 虚拟货币      │
├──────────────────────────────────────────────┤
│ 名称   数量   成本价  当前价  市值   盈亏    │
│ BTC    0.05   500000  530000  26500 +1500   │
│ AAPL   10     180     190     1900  +100    │
└──────────────────────────────────────────────┘
```

---

## 4. AI 报告页

```text
┌──────────────────────────────────────────────┐
│ 日报 / 周报 / 月报                           │
├──────────────┬───────────────────────────────┤
│ 05-28 日报   │ 今日财务复盘                  │
│ 05-27 日报   │ 净资产 +216.30                │
│ 05-26 日报   │ 支出 126.50                   │
│              │ 投资收益 +342.80              │
│              │ 餐饮消费略高                  │
└──────────────┴───────────────────────────────┘
```

---

# 五、推荐落地顺序

最推荐先做：

```text
1. 登录注册
2. 账户管理
3. 分类管理
4. 收支流水
5. 首页资产概览
6. 统计图表
7. 投资持仓
8. AI 日报
9. 预算
10. 资产目标
```

原因：

- 先保证用户能记账。
- 再保证能看到资产变化。
- 最后再加 AI 和投资增强功能。

---

# 六、MVP 验收与部署准备

## 1. 开发测试数据

- 表结构脚本：`xoassets-server/src/main/resources/db/schema.sql`。
- 开发数据脚本：`xoassets-server/src/main/resources/db/dev-data.sql`。
- 测试账号：`demo / xoassets123`。
- `dev-data.sql` 必须包含测试用户、账户、分类、收入 / 支出 / 转账 / 退款流水、投资资产、持仓、投资交易、行情快照、预算、目标和 AI 模板报告。
- 开发数据必须保持金额自洽：账户余额等于初始余额叠加流水影响；DOGE 持仓 `881.3220 * 0.72432000 = 638.3592`。

## 2. 自动化测试重点

- 流水：收入增加余额、支出减少余额、转账双边调整、删除反向恢复。
- 账户：余额修正生成调整记录，账户账本和日终余额曲线可对账。
- 投资：买入后移动平均成本、卖出数量校验、基金确认份额保留 10 位、历史投资快照按交易流水重建、收益剔除净入金。
- 预算：退款抵扣支出，转账不计入预算。
- 首页 / 统计：总资产等于账户余额加投资市值，收支趋势排除转账。
- 数据隔离：账户、分类、流水、持仓、预算、目标均按当前 `user_id` 查询和修改。

## 3. Docker 本地启动

- 仓库根目录执行 `docker compose up -d`。
- MySQL 8 首次启动自动执行 `schema.sql` 和 `dev-data.sql`。
- 后端容器读取 `XOASSETS_DATASOURCE_URL`、`XOASSETS_DB_USERNAME`、`XOASSETS_DB_PASSWORD` 和 JWT 环境变量。
- 后端运行环境通过 `XOASSETS_PROFILE` 切换，默认 `dev`；日志默认写入 `logs/`，可通过 `XOASSETS_LOG_PATH` 调整目录。
- 前端使用 Nginx 提供静态资源，并将 `/api` 代理到后端。
- 访问地址：前端 `http://localhost:8088`，后端和 Knife4j `http://localhost:8080`。

---

# 七、项目一句话总结

> 小〇财迹 XOAssets 是一个以净资产变化为核心的个人财务复盘系统，通过记账、持仓、图表和 AI 报告，帮助用户每天理解自己的钱是怎么变化的。
