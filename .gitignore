# 依赖目录由 npm install 生成，不纳入源码管理。
node_modules/

# Vite 构建产物只用于本地验证和部署输出。
dist/

# Maven 构建产物只用于本地验证和打包输出。
target/

# 后端运行日志由 logback 按天生成，不纳入源码管理。
logs/

# TypeScript 增量构建缓存不属于源码。
*.tsbuildinfo

# IDE、本地环境和系统文件不进入项目。
.idea/
.env
.DS_Store

.playwright-cli
/canvas/
