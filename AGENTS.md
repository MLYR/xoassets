# AGENTS 协作规范

## 1. 项目定位

XOAssets / 小〇财迹 是面向个人用户的资产管理与财务复盘工具。

核心目标：

* 帮助用户每天知道钱花哪了
* 帮助用户知道资产涨跌多少
* 帮助用户知道净资产为什么变化
* 帮助用户进行轻量财务复盘

项目整体由以下部分组成：

```text
xoassets-server   后端服务
xoassets-web      Web 管理端 / Web 前端
xoassets-mobile   Flutter 新版移动端 App
```

如实际目录名称与上述不同，Codex 必须先说明当前目录结构，不允许擅自重命名或迁移目录。

---

## 2. 当前权威文档

全项目文档优先级如下：

1. 根目录 `AGENTS.md`
2. 各子项目目录下的 `AGENTS.md`
3. 移动端阶段文档 `MOBILE_APP_PHASES.md`
4. 产品、设计、开发文档
5. Swagger / Knife4j / 后端接口文档
6. 当前代码实现

如果文档之间出现冲突：

* 先说明冲突点
* 说明影响范围
* 说明风险
* 采用最小必要修改
* 高风险场景先询问用户

---

## 3. 总体实施原则

1. 先读代码和相关文档，再改代码。
2. 优先最小改动，禁止无关重构。
3. 不修改与任务无关文件。
4. 不新增不必要依赖。
5. 不删除、不改写、不搬动原有注释，除非对应逻辑已实际修改且注释失真。
6. 每次更改代码都要写必要注释。
7. 注释应解释业务原因或非显然逻辑，避免空话。
8. 不确定且高风险时先澄清。
9. 低风险场景可做合理假设并推进，但必须说明假设。
10. 每次完成后必须说明改动内容、验证情况和剩余风险。

---

## 4. 技术栈约定

### 4.1 后端

后端技术栈：

* Java 17
* Spring Boot 3.x
* MyBatis-Plus
* MySQL 8
* JWT
* XXL-JOB
* Lombok
* Knife4j / Swagger
* Redis

后端规则：

* 日志使用 `logback-spring.xml`。
* 日志按 `XOASSETS_PROFILE` 区分 `dev / test / prod`。
* dev 环境可输出业务、MyBatis、JDBC 调试日志。
* 生产环境不要开启 SQL DEBUG。
* Redis 已用于股票和虚拟货币投资行情最近 3 天原始快照。
* 除短期行情缓存外，非必要不新增 Redis 使用范围。

---

### 4.2 Web 前端

Web 前端技术栈：

* Vue 3
* Vite
* TypeScript
* Element Plus
* ECharts
* Pinia
* Axios
* Vue Router

Web 端目录：

```text
xoassets-web/
```

Web 端规则：

* Element Plus 继续作为 Web 基础组件库。
* ECharts 继续作为 Web 图表库。
* Web 端必须建立并使用 `xo-design` tokens。
* `xo-design` tokens 需要统一覆盖 Element Plus 的颜色、圆角、阴影、按钮、输入框、表格、卡片、弹窗和分段控件。
* Web 真实接口统一放在 `xoassets-web/src/services/*Api.ts`。
* Web 公共请求逻辑放在 `xoassets-web/src/services/http.ts`。
* Web 主题切换由 `xoassets-web/src/stores/theme.ts` 管理。
* Web 页面局部样式和 ECharts 配置应读取主题 token。
* 禁止为单个页面重复造独立视觉规则。
* 禁止引入新的重型 UI 框架。

---

### 4.3 Flutter 新版移动端

新版移动端技术栈：

* Flutter
* Dart
* Material 3
* 自研 XO Design System
* Riverpod
* go_router
* Dio
* flutter_secure_storage
* shared_preferences

新版移动端目录：

```text
xoassets-mobile/
```

如果实际目录不是 `xoassets-mobile/`，Codex 必须先说明，不允许擅自迁移。

新版移动端权威文档：

```text
xoassets-mobile/AGENTS.md
xoassets-mobile/MOBILE_APP_PHASES.md
```

如果 `MOBILE_APP_PHASES.md` 放在移动端前端根目录下，则移动端开发必须优先读取该文件。

新版移动端规则：

* 移动端从零开始，不复用旧 uni-app 代码。
* 移动端不复用 Web 页面代码。
* 移动端 UI 使用 Flutter Material 3 + 自研 XO Design System。
* 第一版不做离线能力。
* 第一版不引入 SQLite。
* 第一版不引入 Drift。
* 第一版不引入 Hive。
* 第一版不引入 Isar。
* 第一版不引入大型 UI 组件库。
* App 不允许直接连接 MySQL。
* App 只能通过后端 HTTP API 获取数据。
* Token 使用 `flutter_secure_storage` 保存。
* 普通配置使用 `shared_preferences` 保存。

正确架构：

```text
Flutter App
  ↓ HTTP API
Spring Boot 后端
  ↓
MySQL
```

---

### 4.4 旧移动端说明

旧移动端如存在：

```text
xoassets-app/
```

默认视为旧 uni-app 移动端目录。

规则：

* 旧 uni-app 移动端不再作为新版 App 的主要迭代方向。
* 未经用户明确要求，不要修改旧 uni-app 目录。
* 不要把旧 uni-app 约束用于 Flutter 新版移动端。
* 不要把旧 uni-app 文件迁移到 Flutter 项目中。
* 不要删除旧 uni-app 目录，除非用户明确要求。

---

## 5. 模块边界

后端模块建议按业务域拆分：

```text
com.xoassets
├── common
├── module
│   ├── auth
│   ├── account
│   ├── category
│   ├── transaction
│   ├── investment
│   ├── budget
│   ├── goal
│   ├── report
│   ├── dashboard
│   └── statistics
└── persistence
```

规则：

* 后端业务层使用 `service` 接口 + `service/impl` 实现类结构。
* Controller 优先依赖 Service 接口。
* 跨模块调用优先依赖接口。
* 不要在 Controller 中堆复杂业务逻辑。
* 不要在 Mapper XML 中堆复杂业务判断。
* 事务边界放在 Service 层。

---

## 6. 第一版功能边界

### 6.1 第一版必须优先完成

* 登录注册
* 账户管理
* 分类管理
* 收支记账
* 转账
* 首页仪表盘
* 图表统计

### 6.2 第一版建议完成

* 投资持仓手动维护
* 每日汇总
* AI 日报
* 预算管理
* 资产目标

### 6.3 第一版不建议完成

* 银行卡自动同步
* 支付宝自动同步
* 微信自动同步
* 券商自动同步
* 投资买卖建议
* 社交功能
* 复杂后台管理
* 移动端离线同步
* 移动端本地数据库

---

## 7. 业务与数据口径

### 7.1 用户隔离

* 所有用户业务表必须带 `user_id`。
* 所有查询、修改、删除必须通过当前登录用户隔离数据。
* 任何用户不得访问其他用户的数据。
* CSV 导出只允许导出当前登录用户自己的数据。
* 预算、资产目标、AI 报告、交易流水、账户、持仓均必须按当前 `user_id` 隔离。

---

### 7.2 资产口径

```text
总资产 = 现金类账户余额 + 投资资产市值
总负债 = 信用卡负债 + 借款 + 贷款
净资产 = 总资产 - 总负债
今日净资产变化 = 今日净资产 - 昨日净资产
```

规则：

* 首页和统计总资产口径优先使用资产快照。
* 没有快照时页面可退回当前实时概览。
* `/api/snapshots/latest` 缺少较昨日或较月初基准快照时，后端必须返回 `null`。
* 前端展示 `--`，不能用 0 冒充缺失对比。

---

### 7.3 收支口径

* 新增支出：扣减账户余额。
* 新增收入：增加账户余额。
* 转账：不计入收入支出，只影响账户余额，不影响净资产。
* 退款：抵扣支出。
* 预算使用额从 `xo_transaction` 汇总。
* 转账不计入预算使用额。

---

### 7.4 投资口径

```text
投资浮动盈亏 = 当前市值 - 持仓成本
投资收益率 = 浮动盈亏 / 持仓成本 * 100%
```

规则：

* 投资买入 / 卖出必须选择当前用户资金账户。
* 买入扣减账户余额。
* 卖出增加账户余额。
* 投资交易、账户、持仓更新必须在同一事务内完成。
* 投资交易不写入普通流水。
* 投资交易补录、基金确认或撤销后必须从交易日起重建投资日快照和资产快照。
* 前端展示市值、成本和盈亏必须使用后端返回字段。
* 前端不得用格式化后的当前价反算市值、成本或盈亏。
* 投资收益以后端计算为准，前端只展示。

---

### 7.5 投资收益展示口径

基金、股票、虚拟货币收益口径必须分开。

```text
基金：昨日收益
股票：今日收益
虚拟货币：24h 收益
总投资：分项展示，不强行合并为今日收益
```

规则：

* 基金不要展示为“今日收益”。
* 所有资产只有当前价格日期等于今天时才计算今日收益。
* 基金和股票还必须当天为交易日。
* 休市日返回 `todayPriceAvailable=false`。
* 休市日返回 `priceStatus=MARKET_CLOSED`。
* 交易日未更新返回 `priceStatus=TODAY_PRICE_NOT_AVAILABLE`。
* 收益基准价格或基准数量缺失时，对应字段返回 `null`。
* 前端展示 `暂无 / --`。
* 不能用历史价、兜底价或其他值冒充今日价。

---

### 7.6 基金交易口径

* 基金买入默认支持 `AMOUNT_NAV` 金额净值录入。
* 基金可先创建 0 份额持仓。
* `transaction_time` 表示用户实际买入时间。
* 后端通过 `xo_market_calendar` 计算有效申请日和确认日。
* 15:00:00 及以前按当天申请。
* 15:00 后或非交易日顺延。
* 普通基金 T+1。
* 名称包含 `QDII` 的基金 T+2。
* 确认日净值缺失时交易保持 `PENDING_CONFIRM`。
* 后续定时任务确认。
* 股票等资产继续使用 `QUANTITY_PRICE` 数量价格录入。
* 基金金额买入从实际申购日至确认日前按在途投资资产计入。
* 交易后续确认后，补跑确认日前历史快照仍必须保留这段在途金额，避免已扣款但未确认份额导致净资产假跌。

---

### 7.7 持仓规则

* 持仓创建后不允许删除。
* 清仓持仓状态显示“清仓”。
* 列表持有收益展示已实现收益 + 当前浮动盈亏。
* 持仓详情总市值走势使用后端 `chartPoints`。
* 不得用当前 0 份额倒推历史。
* 历史持仓补录必须通过投资买入 / 卖出交易表达。
* 不允许直接修改当前持仓数量倒推历史。
* 手工初始化持仓有后续买卖时，先注入反推手工底仓再应用交易流水。
* 卖出净变化不能提前截断为 0，避免清仓后历史份额丢失。

---

### 7.8 数值精度

* 投资数量统一保留 10 位小数。
* 手续费、成本、市值、盈亏和收益率统一按 4 位小数计算。
* 当前价和日级价保留 8 位。
* CRYPTO 当前价至少展示 6 位。
* FUND / STOCK 展示 4 位。
* 后端返回给前端的 Long ID 必须按字符串处理。
* 前端不得用 `number` 保存业务 ID，避免 JavaScript 精度丢失。
* 移动端金额展示必须统一格式化，最终计算以后端为准。

---

## 8. 核心事务规则

事务必须保证一致性。

### 8.1 普通流水

* 新增支出：保存流水、扣减账户余额、刷新当日汇总必须在同一事务中完成。
* 新增收入：保存流水、增加账户余额、刷新当日汇总必须在同一事务中完成。
* 删除流水：必须处理账户余额反向恢复和每日汇总刷新。
* 修改流水：必须处理原流水反向恢复、新流水影响和每日汇总刷新。

### 8.2 转账

* 转账保存流水、转出扣款、转入加款、刷新汇总必须一致提交。
* 转账不计入收入支出。
* 转账账户不能相同。

### 8.3 投资交易

* 投资买入：保存买入交易、扣减现金账户、增加或更新持仓、重算平均成本、刷新汇总。
* 投资卖出：校验持仓数量、保存卖出交易、减少持仓、增加现金账户、计算已实现盈亏、刷新汇总。
* 投资卖出数量超过持仓时必须提示持仓不足。
* 投资交易撤销必须使用 `cost_amount` 反向恢复历史成本。
* 投资交易撤销不物理删除交易。
* 已撤销交易保留展示，但不参与账户资金明细汇总。

---

## 9. 接口约定

### 9.1 通用接口

* 登录注册接口使用 `/api/auth/**`。
* 当前用户接口使用 `/api/auth/me`。
* 首页接口使用 `/api/dashboard/**`。
* 流水接口使用 `/api/transactions/**`。
* 账户接口使用 `/api/accounts/**`。
* 分类接口使用 `/api/categories/**`。
* 图表接口使用 `/api/statistics/**`。
* CSV 导出接口使用 `/api/export/**`。
* 预算接口使用 `/api/budgets/**`。
* 资产目标接口使用 `/api/goals/**`。
* AI 报告接口使用 `/api/reports/**`。
* 资产快照接口使用 `/api/snapshots/**`。

### 9.2 投资接口

* 公共投资资产接口使用 `/api/assets/**`。
* 持仓接口使用 `/api/holdings/**`。
* 投资交易接口使用 `/api/investment-transactions/**`。
* 手动行情接口使用 `/api/quotes/**`。
* 汇率展示接口使用 `/api/exchange-rates/**`。
* 第三方行情只能由后端调用。
* 前端只调用 XOAssets `/api/quotes/**`。
* 前端只读后端缓存汇率，不直连第三方汇率源。

### 9.3 账户详情接口

* 账户资金明细接口使用 `/api/accounts/{id}/ledger`。
* 账户详情统计使用 `/api/accounts/{id}/flow-statistics`。
* 余额修正使用 `/api/accounts/{id}/balance-adjustments`。
* 账户余额曲线使用 `/api/accounts/{id}/balance-trend`。
* 账户详情只展示当前余额、累计流入、累计流出、余额修正、余额曲线和支出分类。
* 账户详情不再展示投资资金流向图。

### 9.4 接口变更规则

* 接口新增或调整时，同步考虑 Swagger / Knife4j 文档。
* 前端和移动端不得绕过后端接口直接访问数据库。
* 后端返回金额字段时应保持精度。
* 前端和移动端展示金额时使用统一格式化。
* 后端错误信息可展示给前端，但不能暴露敏感信息或第三方原始异常。

---

## 10. 行情与快照规则

### 10.1 行情来源

行情刷新通过 `QuoteProvider` 扩展。

* CRYPTO 使用 CoinGecko。
* FUND 使用天天基金 F10 历史净值表和实时净值兜底。
* A 股使用新浪行情。
* 美股使用 Yahoo Finance。
* 第三方行情只能由后端调用。
* 前端和移动端不直接调用第三方行情源。

### 10.2 行情缓存 TTL

* CRYPTO：15 分钟。
* STOCK：15 分钟。
* FUND：1 天。
* MANUAL：不过期。

### 10.3 Redis 原始快照

* 股票和虚拟货币原始快照写入 Redis ZSET。
* Key 格式：`price:snapshot:{assetId}:{yyyyMM}`。
* TTL：3 天。
* 读取某天原始快照必须通过当天起止毫秒 score 范围查询。
* 禁止全量拉取整月 ZSET 后内存筛选。

### 10.4 日级价格与收益

* `xo_asset_price_daily` 保存长期日级价格。
* `xo_investment_holding_daily_profit` 保存每个持仓展示日真实收益。
* `xo_investment_daily_snapshot` 保存用户投资日快照。
* 每日收益日历、昨日收益、投资趋势每日收益和收益贡献必须从 `xo_investment_holding_daily_profit` 聚合。
* 缺行时显示 `--`。

### 10.5 资产快照

* 资产快照写入 `xo_asset_snapshot`。
* 同一用户同一天重复生成必须更新原记录。
* 现金资产必须按账户初始余额 + 快照日前普通流水 / 投资交易 / 余额修正重建历史余额。
* 正余额计入现金资产。
* 负余额绝对值计入负债。
* 不能用当前 `xo_account.balance` 回填历史。
* 投资资产必须通过 `InvestmentPositionHistoryService.positionsAt(snapshotDate)` 重建快照日历史头寸。
* 投资估值使用同币种日级价 / 当前价。
* 不能用当前 `xo_holding` 数量回填历史。

---

## 11. 市场日历规则

* `xo_market_calendar` 是交易日判断的数据库权威来源。
* 年度补齐任务只生成基础周末日历。
* 交易所特殊休市日通过迁移脚本或人工修正写入数据库。
* 禁止把年度休市日写死在 Java 或 yml。
* 股票只在开盘日 09:30-15:00 之间拉取第三方行情。
* 虚拟货币全天每 15 分钟刷新。
* 刷新失败保留最近价格。
* 定时刷新失败不能影响应用启动。

---

## 12. AI 报告规则

* AI 报告当前为模板化财务复盘。
* AI 报告基于真实统计数据生成。
* AI 报告保存到 `xo_ai_report`。
* 当前不调用真实 AI。
* 不提供投资买卖建议。
* AI 请求前尽量脱敏个人财务数据。
* AI 报告生成失败时标记失败，并支持手动重试。

---

## 13. 前端设计约定

### 13.1 Web 端设计

Web 整体风格：

```text
简洁
专业
清晰
数据感
轻量
安全感
```

Web 视觉基线：

* 现代金融 SaaS 风格。
* 浅灰蓝背景。
* 蓝色主色。
* 白色玻璃卡片。
* 柔和阴影。
* 大圆角。
* 宽松留白。
* 夜间模式使用深色玻璃卡片、弱边框和低饱和高光。

Web 规则：

* 后续前端迭代优先延续现有双主题。
* 不要回退到朴素后台风。
* 不要回退到旧深色侧栏方案。
* Web 第一版采用左侧菜单 + 顶部用户信息 + 主内容区。
* 登录页和业务页视觉参考统一以 `xoassets-web/原型图/` 下的设计图为准。
* 做 UI 调整时优先复用现有布局骨架和自定义 CSS。
* 不引入新的重型 UI 框架。

---

### 13.2 Flutter 移动端设计

Flutter 移动端整体风格：

```text
温和金融
卡片化
清晰
干净
克制
移动端优先
安全感
```

Flutter 移动端禁止风格：

```text
交易所风格
博彩风格
过度科技感
过重玻璃拟态
颜色过多
图表堆叠
Web 页面缩小版
后台管理系统风格
```

Flutter 移动端规则：

* UI 使用 Flutter Material 3 + 自研 XO Design System。
* 所有颜色、圆角、间距、字体必须由 Design System 统一管理。
* 页面容器优先使用 `XoPage`。
* 卡片优先使用 `XoCard`。
* 金额展示必须使用 `XoMoneyText`。
* 空状态使用 `XoEmpty`。
* 加载状态使用 `XoLoading`。
* 错误状态使用 `XoErrorView`。
* 不允许页面内到处写死颜色、圆角和字号。
* 不允许直接套大型 UI 库。
* 不允许将 Web 页面缩小后搬到移动端。

---

## 14. 移动端底部导航规则

Flutter 新版移动端底部导航：

```text
首页
记账
中间悬浮 +
投资
我的
```

中间 `+` 操作：

```text
记一笔
转账
投资交易
新增账户
新增预算
```

移动端页面优先级以 `xoassets-mobile/MOBILE_APP_PHASES.md` 为准。

---

## 15. 移动端阶段约束

Flutter 新版移动端必须按阶段推进。

阶段顺序：

```text
阶段 0：基础约束与项目准备
阶段 1：App 壳子
阶段 2：登录与基础 API
阶段 3：首页资产驾驶舱
阶段 4：记账与流水录入
阶段 5：账户管理
阶段 6：投资管理
阶段 7：预算管理
阶段 8：AI 财务报告
阶段 9：设置与系统能力
阶段 10：附件与图片上传
阶段 11：图表与分析
阶段 12：性能优化与体验打磨
阶段 13：Android / iOS 上架准备
阶段 14：自动化与质量保障
```

规则：

* 每次只执行用户指定阶段。
* 不允许越阶段实现。
* 不允许一次性实现所有业务。
* 不允许未确认就引入新依赖。
* 不允许修改与当前阶段无关的模块。
* 阶段细节以移动端目录下 `MOBILE_APP_PHASES.md` 为准。

---

## 16. 安全要求

* 使用 JWT 登录认证。
* Token 放在 `Authorization` Header。
* Header 格式：`Authorization: Bearer <token>`。
* 后端所有查询、修改、删除必须校验当前登录用户 ID 与数据所属 `user_id` 一致。
* 密码禁止明文存储，使用 BCrypt 加密。
* 日志中不要打印金额明细。
* 日志中不要打印账户信息。
* 日志中不要打印 Token。
* 日志中不要打印密码。
* 日志中不要打印 AI 请求敏感内容。
* 运行日志写入 `logs/`。
* `logs/` 必须加入 `.gitignore`。
* 提交前不要把本地日志文件加入 Git。
* 外部输入必须校验。
* 异常路径要显式处理。
* App 不允许写死生产密钥。
* App 不允许写死 MySQL 账号密码。
* App 不允许直连对象存储密钥。

---

## 17. 异常处理要求

必须处理以下异常：

* 金额为空：提示金额必填。
* 金额小于等于 0：提示金额必须大于 0。
* 账户不存在：提示账户不存在。
* 分类不存在：提示分类不存在。
* 转账账户相同：不允许同账户转账。
* 投资卖出数量超过持仓：提示持仓不足。
* 行情获取失败：使用最后一次可用价格，并标记价格过期。
* AI 报告生成失败：标记失败，并支持手动重试。
* 登录过期：引导重新登录。
* 网络异常：提示网络异常，并保留重试入口。
* 空数据：展示空状态，不要展示报错。
* 后端返回 `null` 的金额或收益字段：展示 `--`，不要用 0 替代。

---

## 18. 定时任务

定时任务统一由 XXL-JOB Admin 触发。

规则：

* 后端仅注册 executor handler。
* 本地未启动 XXL-JOB Admin 或未开启 `XXL_JOB_EXECUTOR_ENABLED=true` 时不会自动执行定时任务。

定时任务约定：

* 股票行情同步：开盘日 09:30-15:00 每 15 分钟。
* 虚拟货币行情同步：全天每 15 分钟。
* 基金净值晚间刷新：每天 18:00 首轮，18:15-23:45 每 15 分钟强制跟进刷新。
* 资产日级价格聚合：每天 20:00-23:45 每 15 分钟处理晚间汇总和收尾汇总。
* 投资资产日快照：每天 20:00 首轮，20:15-23:45 每 15 分钟先重建持仓每日收益，再 upsert 最近 4 个自然日投资快照。
* 资产快照：每天 20:00-23:45 每 15 分钟生成所有启用用户当天快照，最后一轮为 23:45。
* 待重建资产快照处理：每天 10:00、15:00、21:00 处理 `xo_snapshot_rebuild_task`。
* 基金待确认交易扫描：每 3 小时扫描一次。
* 每日汇总：每天 00:10 生成昨日财务汇总。
* AI 日报：每天 00:20 生成昨日 AI 报告。
* 预算检查：每天 09:00 检查预算超支。
* 价格过期检查：每小时检查行情是否过期。

---

## 19. 验证要求

### 19.1 后端验证

后端改动优先运行相关单元测试或最小范围测试。

建议命令：

```bash
./mvnw test
```

如果当前环境不适合运行测试，需要说明原因和风险。

---

### 19.2 Web 验证

Web 改动优先运行：

```bash
cd xoassets-web
npm install
npm run lint
npm run typecheck
npm run build
```

如果项目没有对应脚本，说明实际可用脚本。

---

### 19.3 Flutter 移动端验证

Flutter 移动端改动优先运行：

```bash
cd xoassets-mobile
flutter pub get
flutter analyze
flutter test
flutter run
```

如果 iOS 环境不可用，需要说明原因。

第一阶段不要求打包上架，但后续上架阶段需要验证：

```bash
flutter build apk
flutter build appbundle
flutter build ios
```

---

## 20. Git 约定

未经用户明确要求，不执行：

```bash
git add
git commit
git push
git checkout -b
git branch
创建 PR
```

写完代码后只汇报：

* 改动内容
* 验证结果
* 剩余风险
* 是否建议提交

用户要求提交到 Git 时，默认使用中文提交描述。

推荐 Commit 格式：

```text
type(scope): summary
```

示例：

```text
feat(mobile): 初始化 Flutter App 壳子
fix(server): 修复投资快照重建口径
docs(agent): 更新根目录协作规范
```

阶段性功能完成后：

* 必须同步相关文档。
* 必须说明是否需要更新 agent 记忆。
* 是否提交 Git 等待用户确认。

---

## 21. Codex 执行规则

### 21.1 执行前

Codex 每次执行任务前必须先输出：

```text
当前目录结构分析
已读取的约束文档
准备新增的文件
准备修改的文件
本次执行计划
风险点
需要用户确认的问题
```

未经用户确认，不要直接大范围修改代码。

---

### 21.2 执行中

Codex 必须遵守：

```text
只做当前任务
只做当前阶段
不越阶段实现
不引入未批准依赖
不删除无关文件
不修改无关模块
不做顺手重构
不批量格式化无关文件
不修改后端以外的无关项目
不修改 Web 以外的无关项目
不修改移动端以外的无关项目
```

---

### 21.3 执行后

Codex 必须输出：

```text
完成内容
新增文件
修改文件
删除文件，若有
新增依赖，若有
运行命令
验证结果
遗留问题
风险点
下一步建议
```

---

## 22. 依赖管理规则

新增依赖必须说明：

* 依赖名称
* 用途
* 为什么需要
* 是否有替代方案
* 是否影响包体积
* 是否影响构建
* 是否为长期维护依赖

未经过用户确认，不要引入重型依赖。

### 22.1 Web 端依赖

Web 端继续使用现有技术栈，非必要不新增 UI 库。

禁止：

```text
新增重型 UI 框架
替换 Element Plus
替换 ECharts
无关状态管理库
```

### 22.2 Flutter 移动端依赖

第一阶段允许：

```text
flutter_riverpod
go_router
dio
flutter_secure_storage
shared_preferences
intl
```

第一阶段禁止：

```text
sqlite
drift
hive
isar
大型 UI 组件库
```

---

## 23. 本地开发约定

### 23.1 Docker

本地 Docker 一键启动使用：

```bash
docker compose up -d
```

默认访问：

```text
Web Nginx: http://localhost:8088
后端服务: http://localhost:8080
Knife4j: http://localhost:8080
```

### 23.2 后端初始化数据

MVP 验收数据位于：

```text
xoassets-server/src/main/resources/db/dev-data.sql
```

全新库初始化顺序：

```text
schema.sql
migration-market-calendar.sql
dev-data.sql
```

测试账号：

```text
demo / xoassets123
```

---

## 24. 移动端环境说明

Flutter 移动端默认开发 API：

```text
http://localhost:8080/api
```

Android 模拟器访问宿主机本地服务时，可能需要：

```text
http://10.0.2.2:8080/api
```

iOS 模拟器通常可以使用：

```text
http://localhost:8080/api
```

具体以后端运行环境为准。

---

## 25. 不允许做的事

以下行为默认禁止：

* 未经确认删除文件。
* 未经确认大规模重构。
* 未经确认修改目录结构。
* 未经确认引入新框架。
* 未经确认替换技术栈。
* App 直接连接 MySQL。
* 前端直接调用第三方行情源。
* 前端直接调用第三方汇率源。
* 前端保存对象存储密钥。
* 移动端第一版做离线同步。
* 移动端第一版引入本地数据库。
* 把 Web 页面直接缩小成移动端页面。
* 把旧 uni-app 规则套用到 Flutter 新版移动端。
* 把旧 uni-app 代码迁移到 Flutter 项目。
* 把后端金额计算逻辑复制到前端或移动端。
* 用 0 代替后端返回的 `null` 金额或收益字段。
* 用历史价或兜底价冒充今日价格。
* 给用户提供投资买卖建议。

---

## 26. 沟通风格

* 简洁直接。
* 先给结论，再解释原因。
* 能给步骤就给实际步骤。
* 能给代码就给代码。
* 遇到风险点直接提醒。
* 不确定的地方明确说。
* 不要编造。
* 不要过度扩展无关内容。
* 不要默认用户需要教程。
* 技术问题优先给可落地方案。

---

## 27. 长期原则

1. 后端是业务和数据口径权威。
2. Web 端是已有管理端 / Web 前端。
3. Flutter 是新版移动端方向。
4. 旧 uni-app 不再作为新版移动端依据。
5. 移动端第一版不做离线。
6. 移动端不直接连接数据库。
7. 前端和移动端只展示后端返回的业务结果。
8. 金额和收益口径以后端为准。
9. 基金、股票、虚拟货币收益口径必须分开。
10. 首页不要堆复杂图表。
11. 记账页移动端优先使用日历视图。
12. AI 报告不是聊天首页。
13. Codex 每次只做当前任务。
14. 每次改动前必须输出计划。
15. 每次改动后必须输出验证结果。
